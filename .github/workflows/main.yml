name: Run News Bot
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run
        env:
          GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
        run: python main.py
